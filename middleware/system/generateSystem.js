/**
 * Generál egy véletlenszeerű csillagrendszert, véletlen számú véletlen bolygóval
 * @param {*} objectrepository 
 * @returns 
 */
module.exports = function (objectrepository) {
    return function (req, res, next) {
        next();
    };
};